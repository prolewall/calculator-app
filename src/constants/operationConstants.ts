export enum MathematicalOperation {
    Add = "+",
    Subtract = "-",
    Multiply = "x",
    Divide = "/"
}

export enum CalculatorOperation {   
    Delete = "DEL",
    Reset = "RESET",
    Calculate = "="
}